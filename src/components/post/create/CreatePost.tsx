import {PureComponent} from "react";
import {Link} from "react-router-dom";
export default class CreatePost extends PureComponent {
    render() {
        return (<div className={"postSearch"}>
            <Link to={}>create post</Link>
        </div>);
    }
}